#include <stdio.h>
#include <stdlib.h>

int main()
{
    int base;
    int altura;
    int perimetro;

    printf("Ingrese la base: ");
    scanf("%d", &base);

    fflush(stdin);

    printf("Ingrese una altura: ");
    scanf("%d", &altura);

    printf("Perimetro: %d", perimetro);

    return 0;
}
